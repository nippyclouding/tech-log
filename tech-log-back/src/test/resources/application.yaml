spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: test-github-client-id
            client-secret: test-github-client-secret
            scope:
              - read:user
              - user:email

app:
  admin-console:
    username: admin
    password: test-admin-password
  image:
    upload-dir: build/test-image
    public-path: /image
    max-total-size: 20MB
