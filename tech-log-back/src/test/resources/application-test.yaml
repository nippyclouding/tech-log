spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: test-github-client-id
            client-secret: test-github-client-secret
            scope:
              - read:user
              - user:email
  mail:
    host: localhost
    port: 2525

app:
  admin-console:
    username: admin
    password: test-admin-password
  frontend-origin: http://localhost:3000
  image:
    upload-dir: build/test-image
  mail:
    enabled: false
    from: test@example.com
    site-name: Test Tech Log
